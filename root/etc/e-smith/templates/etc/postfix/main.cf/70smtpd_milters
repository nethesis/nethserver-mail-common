#
# SMTPD_MILTERS
#
# default action: accept (skip milters if something goes wrong)
# else set to 'reject'

milter_default_action = {$postfix{milterAction} || 'accept'}

#
# 70smtpd_milters -- see 01smtp_milters_* fragment for default array settings..
#
smtpd_milters = { join (",\n ", @smtpd_milters); }

